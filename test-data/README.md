# 测试数据约定

此目录只允许保存完全虚构、可公开的 HealthLife 演示数据。

- 不得复制浏览器导出的真实个人健康备份。
- 示例中的日期、体重、睡眠和备注不得对应真实用户。
- `healthlife-demo-v1.json`、`healthlife-demo-v2.json` 和 `healthlife-demo-v3.json` 是历史版本的虚构样例；当前应用只接受 schema v5，不读取这些旧样例。
