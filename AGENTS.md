# HealthLife 项目规范

## 项目定位

- HealthLife 是面向个人使用的手机端健康追踪工具，覆盖运动、饮食、睡眠和体重四类日常记录。
- 正式站点入口规划为 `docs/index.html`，通过 GitHub Pages 发布，并支持添加到主屏幕和离线访问。
- 项目坚持本地优先：不提供账号、服务器或自动同步，正式数据只保存在当前浏览器中。
- 用户可通过完整 JSON 备份迁移和恢复数据；仓库、演示数据和测试数据不得包含真实个人健康信息。
- 产品只用于个人记录和趋势观察，不提供疾病诊断、治疗建议或其他医疗结论。

## MVP 范围

### 今日

- 展示所选日期的运动、饮食、睡眠和体重记录状态。
- 支持从日历切换日期和补录历史记录。
- 提供四类记录的快速入口，并展示连续记录情况。

### 运动

- 记录运动类型、时长、强度和备注。
- 初始类型为力量、有氧、步行、拉伸、球类和其他。
- MVP 只追踪一次运动的整体情况，不记录动作、组数、次数和负重。

### 饮食

- 记录早餐、午餐、晚餐和加餐。
- 每条记录包含文字描述、健康程度、饱腹感和可选备注。
- 可记录每日饮水量。
- MVP 不接入食物数据库，不计算卡路里和宏量营养素。

### 睡眠

- 记录入睡时间、起床时间、睡眠质量、可选夜醒次数和备注。
- 自动计算跨日睡眠时长，异常或不完整时间不得生成误导性结果。

### 体重

- 记录日期、体重、可选体脂率和备注。
- 每个自然日最多保留一条正式体重记录，重复录入时进入编辑流程。
- 趋势优先展示 7 日均重，单日体重只作为原始数据，不输出健康判断。

## 页面与导航

- 一级导航保持为“今日、趋势、记录”三个入口。
- “今日”负责日历、当日概览和快速记录。
- “趋势”提供周／月维度的体重、睡眠、运动和饮食汇总。
- “记录”按日期展示全部明细，并提供查看、编辑和删除。
- “设置／数据”集中管理个人目标、显示单位、完整备份和恢复。
- 界面以 390px 左右手机视口为主要验收尺寸；高频触控目标不得小于 44px，表单输入字号不得低于 16px。

## 技术约定

- 使用原生 HTML、CSS 和 JavaScript，不引入前端框架或第三方运行时依赖。
- `docs/` 是 GitHub Pages 发布目录；正式页面使用相对路径，运行时不得依赖第三方网络资源。
- PWA 资源必须覆盖正式应用外壳，并在缓存版本变化时正确更新。
- `tests/` 使用 Node.js 标准库验证数据模型、统计、校验和备份恢复；测试不得要求网络访问。
- `test-data/` 只允许保存虚构演示数据，禁止加入真实健康备份。
- 业务计算尽量写成与 DOM 和存储无关的纯函数。
- 用户输入不得直接拼入 `innerHTML` 或内联事件处理器；优先使用安全 DOM API、事件监听器和 `data-*` 属性。

## 数据约定

- 所有业务记录使用 UUID，不使用时间戳作为唯一标识。
- 所有持久化对象必须带 schema 版本；读取和导入时必须校验版本、字段、范围及跨对象引用。
- 日期使用本地自然日字符串 `YYYY-MM-DD`；时间使用 `HH:mm`，不得使用 UTC 转换改变用户选择的日期。
- 时长统一保存为整数分钟。
- 体重统一以整数克保存，界面层负责公斤或其他显示单位转换，避免浮点累计误差。
- 评分和等级使用固定整数枚举，数据层不得保存仅用于展示的文字标签。
- 每次新增或修改记录都保存 `createdAt` 和 `updatedAt`，时间戳使用 ISO 8601 格式。
- 修改数据模型时必须同时设计旧数据迁移以及导入、导出兼容策略。
- 本地保存失败必须向用户显示并阻止界面假装成功，不得静默忽略。
- 导入执行前必须展示摘要并整体校验；MVP 采用完整替换，不做自动合并。
- 覆盖已有本地数据前应自动生成或明确提示用户导出当前完整备份。
- 删除记录默认提供短时间撤销；持久化失败时必须回滚界面状态。

## 统计口径

- 周／月统计均以用户本地时区和自然日为边界。
- 体重趋势使用已有记录计算 7 日移动平均，不用缺失日期补零。
- 睡眠统计以起床日期归属自然日，跨日时长根据入睡和起床时间计算。
- 运动汇总只统计有效记录的次数和整数分钟数。
- 饮食 MVP 只汇总记录完整度、健康程度和饱腹感，不推导卡路里或营养摄入。
- 数据不足时明确显示样本量或“暂无足够数据”，不得生成确定性健康结论。

## 修改原则

- 只修改当前任务直接涉及的文件和逻辑，不顺手重构或格式化无关内容。
- 不添加需求之外的功能、抽象、配置项或兼容层。
- 新依赖必须先证明标准库和已有代码无法满足，并在引入前取得用户确认。
- 修复缺陷时优先建立可复现步骤或回归测试，再修改实现。
- 涉及数据模型的改动必须覆盖无效输入、边界条件、迁移和备份往返验证。
- 完成前至少运行 JavaScript 语法检查、相关自动化测试和手机视口回归。
- 发布到 GitHub Pages、推送远端或其他公开发布操作必须单独取得用户确认。

## 文档维护

- `README.md` 用于项目介绍、使用方法、本地数据边界和备份恢复说明。
- `ROADMAP.md` 是项目真实进度源，记录当前阶段、已完成、进行中、待办、阻塞和最近验证。
- 功能、导航、字段、统计口径或数据管理流程变化时，必须同步更新相关文档。
- 只有已经实现并验证通过的事项才能写入“已完成”；未验证内容保留在“进行中”或“待办”。
- 纯讨论和只读分析不更新路线图；项目状态发生变化后必须同步更新。

