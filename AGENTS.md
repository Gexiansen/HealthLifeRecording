# HealthLife 项目规范

## 项目定位

- HealthLife 是面向个人使用的手机端健康追踪工具，覆盖运动、饮食、睡眠和体重四类日常记录。
- 正式站点入口规划为 `docs/index.html`，通过 GitHub Pages 发布，并支持添加到主屏幕和离线访问。
- 项目坚持本地优先：不提供账号、服务器或自动同步，正式数据只保存在当前浏览器中。
- 用户可通过完整 JSON 备份迁移和恢复数据；仓库、演示数据和测试数据不得包含真实个人健康信息。
- 产品只用于个人记录和趋势观察，不提供疾病诊断、治疗建议或其他医疗结论。

## MVP 范围

### 今日

- 展示所选日期的运动、饮食、睡眠和体重记录状态。
- 支持从日历切换日期和补录历史记录。
- 提供四类记录的快速入口，不使用连续打卡天数评价健康行为。
- 根据简单的每周训练模板展示当天推荐训练，不记录工作安排、加班类型、执行状态或改期关系。
- 日历标题区可以紧凑展示所选日期的训练模板类型和所选月份的实际运动天数；实际运动天数按正式运动记录的自然日去重，不计算计划执行率。
- 日历每日完整度在训练日按运动、饮食、睡眠和体重四项计算；模板休息日且没有实际运动时只按饮食、睡眠和体重三项计算并显示休息标识，若休息日记录了额外运动则恢复四项计算。休息不得生成虚假运动记录，也不得计入实际运动天数。

### 运动

- 记录运动类型、时长、强度、数据来源和备注。
- 普通运动录入按 `keep`、`running`、`other` 三个场景分流；引导式训练生成的正式记录使用独立 `guided` 场景，不出现在普通场景选择器中。
- Keep 跟练保存课程名称、是否完整跟完、可选器械重量和可选整体不适反馈；Keep 课程场景与 `manual`／`appleWatch` 数据来源必须使用不同字段表达。
- 跑步场景固定保存跑步类型，展示时长、强度、数据来源、距离、可选平均心率和配速预览；其他运动场景只展示类型、时长、强度和备注。
- 新建普通运动可以复制最近一次同场景训练到未保存表单；复制不得自动生成正式记录，不复制上次平均心率、不适或备注等单次结果。
- 初始类型为力量、跑步、有氧、步行、拉伸、球类和其他。
- 可选记录 Apple Watch 平均心率和距离；配速根据距离与时长计算，不重复持久化。活动热量和最高心率不进入核心记录。
- 提供内置新手动作库，首批覆盖壶铃杯式深蹲、哑铃罗马尼亚硬拉、俯卧撑、单臂哑铃划船、死虫式和爬楼梯。
- 每个动作固定展示准备姿势、三条关键提示、常见错误和停止条件；提示用于辅助自查，不宣称能够自动判断动作质量或替代专业指导。
- 首页训练计划可以进入引导式训练，按动作和组逐项完成，支持组间休息、跳过动作、中途退出后继续以及结束前总结确认。
- 完成组、跳过动作和提前结束前必须保存可持久化的撤销历史；训练、休息和总结阶段均可撤销上一步，总结阶段以“返回修改”表达。
- 放弃本次训练必须二次确认，只清除当前训练草稿和撤销历史；提前结束并总结也必须二次确认，避免误触跳过全部剩余动作。
- 手动新增的普通运动仍只记录整体情况；通过引导式训练生成的运动记录可以额外保存动作、组数、次数／楼层、负重和主观用力感快照。
- 引导式训练展示同一实际动作最近的完成样本，并使用保守规则提供维持、减量或小幅进阶提示；建议不得自动修改本次目标或器械重量。
- 只有同一动作最近两次均完整完成、主观用力不高且没有不适时，才提示增加每组次数或使用器械可调的最小重量档；样本不足、缩短完成、明显吃力或存在不适时优先维持、减量或停止。
- 训练中允许把当前动作替换为同一动作模式的低门槛替代动作，历史记录必须同时保存原计划动作和实际动作，不得把替换后的完成情况归到原动作。
- 训练总结可以按动作记录可选的不适部位和程度；产品只汇总用户主观反馈，不根据疼痛位置诊断损伤。

### 饮食

- 记录早餐、午餐、晚餐和加餐。
- 家庭饮食可以从个人常用食材中多选，按默认份量快速记录，并允许只修改本餐份量；个人食材在设置中维护，不写死在代码中。
- 每条记录同时保存可读 `content`、自由文字和已选食材历史快照；修改或移除常用食材默认不得改变历史记录。只有用户明确把蛋白质参考值变更认定为纠错、预览影响并确认同步时，才允许修正同一来源食材的兼容历史蛋白质估算。
- 蛋白质估算是可选辅助，必须标记估算范围、口径和未估算内容；不建设热量、脂肪、碳水或公共大型食物库。
- 外卖、堂食和临时食物始终可以只写自由文字；软件不从文字自动推导确定性营养值，也不因无法称重阻塞记录。
- 新增饮食时展示所选日期已有餐次，并允许直接载入原记录继续补充；同一天再次保存早餐、午餐或晚餐时必须明确选择补充原记录或仍新增一条，不得静默生成重复餐次。加餐允许记录多条。

### 睡眠

- 记录入睡时间、起床时间、睡眠质量、可选夜醒次数和备注。
- 自动计算跨日睡眠时长，异常或不完整时间不得生成误导性结果。

### 体重

- 记录日期、体重、可选体脂率和备注。
- 每个自然日最多保留一条正式体重记录，重复录入时进入编辑流程。
- 趋势优先展示 7 日均重，单日体重只作为原始数据，不输出健康判断。

## 页面与导航

- 一级导航保持为“今日、趋势、记录”三个入口。
- “今日”负责日历、当日概览和快速记录。
- 引导式训练作为“今日训练”卡的备用文字训练进入全屏二级流程，不新增一级导航；普通运动记录是主操作。
- “趋势”提供周／月维度的体重、睡眠、运动和饮食汇总。
- “记录”按日期展示全部明细，并提供查看、编辑和删除。
- “设置”使用紧凑首页和二级页面组织功能，不新增一级导航；首页第一屏优先展示完整备份导出、最近备份状态和恢复入口，其后展示分析导出及常用食材、每周训练模板、安装更新入口。
- 完整备份导出在设置首页一键执行；恢复备份、常用食材、每周训练模板和安装更新分别进入二级页面。二级页面返回设置首页，恢复仍须经过文件选择、摘要预览和最终替换确认。
- 界面以 390px 左右手机视口为主要验收尺寸；高频触控目标不得小于 44px，表单输入字号不得低于 16px。

## 技术约定

- 使用原生 HTML、CSS 和 JavaScript，不引入前端框架或第三方运行时依赖。
- `docs/` 是 GitHub Pages 发布目录；正式页面使用相对路径，运行时不得依赖第三方网络资源。
- 新功能开发先按交互不确定性、数据风险和重复实现成本选择验证强度；对于复用现有页面壳和视觉语言、正式实现与原型成本接近的原生 HTML／CSS／JavaScript 改动，优先完成文字产品基线后按纵向切片开发，不为流程形式另建独立原型。
- `PRODUCT_DEFINITION_V11.md` 描述 v11 整体产品目标和后续切片边界；已经实现的事实以 `docs/`、`DATA_MODEL.md` 和 `UX_INTERACTION.md` 为准。
- PWA 资源必须覆盖正式应用外壳，并在缓存版本变化时正确更新。
- `tests/` 使用 Node.js 标准库验证数据模型、统计、校验和备份恢复；测试不得要求网络访问。
- `test-data/` 只允许保存虚构演示数据，禁止加入真实健康备份。
- 业务计算尽量写成与 DOM 和存储无关的纯函数。
- 用户输入不得直接拼入 `innerHTML` 或内联事件处理器；优先使用安全 DOM API、事件监听器和 `data-*` 属性。

## 数据约定

- 所有业务记录使用 UUID，不使用时间戳作为唯一标识。
- 所有持久化对象必须带 schema 版本；读取和导入时必须校验版本、字段、范围及跨对象引用。
- 切片三使用 schema v12 和独立存储键 `healthlife:data:v12`；没有 v12 时优先迁移经过完整校验的 schema v11，缺少 v11 时才迁移 v10，本地数据和完整备份均不接受 v1～v9。
- 日期使用本地自然日字符串 `YYYY-MM-DD`；时间使用 `HH:mm`，不得使用 UTC 转换改变用户选择的日期。
- 时长统一保存为整数分钟。
- 体重统一以整数克保存，界面层负责公斤或其他显示单位转换，避免浮点累计误差。
- 评分和等级使用固定整数枚举，数据层不得保存仅用于展示的文字标签。
- 饮食记录保存 1～2000 字符的可读 `content`、自由文字和最多 50 项食材快照；没有食材时自由文字不得为空。
- 个人常用食材保存名称、分类、默认份量、单位和可选蛋白质参考；饮食快照保存本餐实际份量与当时的蛋白质参考。食材库更新默认只影响以后记录；修改蛋白质参考值且存在历史快照时，必须展示仅影响以后／同步修正历史两种处理方式、影响餐数与估算变化，禁止静默回算。
- 同步修正历史蛋白质估算只允许处理同一 `sourceFoodId`、单位和口径兼容的快照，使用当餐已保存份量重新计算，只更新蛋白质估算及引用元数据和记录 `updatedAt`，不得改写餐食原文、食材名称、份量或自由文字。单位或生熟等口径发生变化时不得批量回算；保存失败不得改变界面状态，成功后提供短时间撤销。
- 蛋白质统一以整数毫克保存，界面层负责克数显示；自由文字和缺少参考值的食材必须计入未估算内容。
- 早餐、午餐和晚餐的同日同餐次冲突只在交互层提示，不增加 schema 唯一约束；用户明确选择后产生的重复记录以及多条加餐仍是有效数据。
- 每次新增或修改记录都保存 `createdAt` 和 `updatedAt`，时间戳使用 ISO 8601 格式。
- schema v10 已经产生正式个人数据，schema v11 已经推送；v12 迁移成功后仍保留 v11 和已有 v10 键。后续修改数据模型必须提供兼容的本地数据与完整备份迁移，不得通过清空数据完成版本切换。
- 本地保存失败必须向用户显示并阻止界面假装成功，不得静默忽略。
- 导入执行前必须展示摘要并整体校验；MVP 采用完整替换，不做自动合并。
- 覆盖已有本地数据前应自动生成或明确提示用户导出当前完整备份。
- 删除记录默认提供短时间撤销；持久化失败时必须回滚界面状态。
- 每周训练模板只用于推荐当天训练，不保存当天计划、完成状态或改期关系，也不得自动生成实际运动记录。
- 进行中的引导式训练使用独立版本化草稿键保存；完成一组后立即更新草稿，本地保存失败时不得在界面中将该组标记为完成。
- 训练撤销历史使用独立版本化存储键，快照必须属于当前草稿且通过完整草稿校验；撤销成功后同步删除已消费快照，刷新页面后仍可继续撤销。
- 引导式训练结束后必须先展示实际完成摘要，由用户确认后生成正式运动记录；跳过、退出或计划打钩不得自动生成正式记录。
- 正式运动记录中的动作详情保存动作名称、计量单位、目标值、实际值和负重快照，内置动作库后续更新不得改变历史记录。
- 普通运动场景和 Keep 详情属于正式运动记录；v11 运动迁移时，引导训练映射为 `guided`、普通跑步映射为 `running`、其余映射为 `other`，不得猜测旧记录来自 Keep。
- 动作替换必须在训练草稿中即时保存；刷新后应继续显示实际替代动作，完成快照同时保存 `plannedExerciseId` 和 `exerciseId`。
- 不适反馈按实际动作保存，包含是否已反馈、固定部位枚举和 1～3 级主观程度；未反馈与明确“没有不适”必须可区分，不得从缺失反馈推断“没有疼痛”。

## 统计口径

- 周／月统计均以用户本地时区和自然日为边界。
- 体重趋势使用已有记录计算 7 日移动平均，不用缺失日期补零。
- 睡眠统计以起床日期归属自然日，跨日时长根据入睡和起床时间计算。
- 运动汇总只统计有效记录的次数和整数分钟数。
- 月度运动天数按所选月份内存在至少一条正式运动记录的自然日去重计算，同一天多条运动只计 1 天。
- Apple Watch 平均心率、距离和配速只按已有运动样本汇总，不对缺失日期补零。
- 饮食统计保留餐数、记录自然日数和覆盖率；蛋白质只汇总带快照参考值的已估算部分，并明确未估算餐次，不从自由文字推导营养素。
- 分析导出保留饮食原文，后续分析必须明确区分用户记录与分析推断，不得生成医学结论。
- 数据不足时明确显示样本量或“暂无足够数据”，不得生成确定性健康结论。

## 修改原则

- 只修改当前任务直接涉及的文件和逻辑，不顺手重构或格式化无关内容。
- 不添加需求之外的功能、抽象、配置项或兼容层。
- 新依赖必须先证明标准库和已有代码无法满足，并在引入前取得用户确认。
- 修复缺陷时优先建立可复现步骤或回归测试，再修改实现。
- 涉及数据模型的改动必须覆盖无效输入、边界条件、迁移和备份往返验证。
- 完成前至少运行 JavaScript 语法检查、相关自动化测试和手机视口回归。
- 发布到 GitHub Pages、推送远端或其他公开发布操作必须单独取得用户确认。

## 文档维护

- `README.md` 用于项目介绍、使用方法、本地数据边界和备份恢复说明。
- `ROADMAP.md` 是项目真实进度源，记录当前阶段、已完成、进行中、待办、阻塞和最近验证。
- `HEALTH_ADVICE_HISTORY.local.md` 用于保存真实的个人健康背景和历次建议，只保留在本地并由 `.gitignore` 排除；提供个性化饮食、运动或恢复建议前必须先读取。
- 当用户补充稳定的个人健康事实，或饮食、训练、睡眠、恢复讨论形成明确结论时，在当前任务中自动按日期追加到该文件，不要求用户再次提醒；临时想法、尚未确认的备选方案和纯软件需求讨论不写入。
- 新信息与历史内容冲突时，追加带日期的修正说明，不静默改写历史结论；每次自动同步后向用户说明写入了什么。该本地个人记录的日常追加不属于产品路线图变更，只有维护规则发生变化时才更新 `ROADMAP.md`。
- 功能、导航、字段、统计口径或数据管理流程变化时，必须同步更新相关文档。
- 只有已经实现并验证通过的事项才能写入“已完成”；未验证内容保留在“进行中”或“待办”。
- 纯讨论和只读分析不更新路线图；项目状态发生变化后必须同步更新。
