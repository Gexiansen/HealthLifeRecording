# HealthLife v11 产品定义

## 文档状态

- 状态：切片一“数据基础”和切片二“家庭饮食闭环”已实现并验证；切片三“运动场景闭环”已完成实现与自动化验证，最终 390×844 浏览器回归待浏览器连接恢复；切片四至五尚未开始。
- 验证方式：文字产品基线后，复用现有原生 HTML、CSS 和 JavaScript，按端到端可体验的纵向切片开发；不单独制作原型图或维护一次性完整原型。
- 当前生产事实：`docs/`、`DATA_MODEL.md` 和 `UX_INTERACTION.md` 已升级到 schema v12，并完成个人常用食材、饮食快记、今日蛋白质估算和运动场景分流；本文档其余内容仍定义后续切片目标。
- 隐私边界：仓库只保存通用产品规则和虚构示例，个人食材、蛋白质目标、阶段目标与健康记录只保存在本地数据中。

## 一、当前事实

- 一级导航为“今日、趋势、记录”，设置作为辅助入口。
- 今日页优先展示日历和运动、饮食、睡眠、体重四类记录入口，内置引导训练位于其后。
- 日历记录完整度在训练日按四项计算；模板休息日无实际运动时按饮食、睡眠和体重三项计算并标记“休”，额外运动仍可正常记录且恢复四项计算。
- 饮食记录保存餐次、可读 `content`、自由文字和食材历史快照；蛋白质只汇总已配置参考值的部分。
- 普通运动按 Keep、跑步和其他运动分流，可复用最近一次同场景训练；备用文字训练另有完整流程。
- 正式数据为 schema v12；有效 v11 本地数据和完整备份优先迁移，缺少 v11 时才链式迁移 v10，原前序版本本地键保留。

## 二、需要解决的问题

1. 家庭饮食由少量固定食材重复组合，但每次仍需重新输入相同名称和份量。
2. 用户下一阶段需要观察蛋白质总量与分餐分布，单纯文字记录无法在应用内形成可靠估算。
3. Keep 跟练、跑步／Apple Watch 和其他运动所需字段不同，通用运动表单没有突出真实使用路径。
4. 内置引导训练与外部视频跟练部分重叠，需要从主入口降为备用能力。
5. 现有趋势可以回顾数据，但缺少一个低负担的“当前阶段”来提醒本阶段重点并在周期结束后复盘。

## 三、产品目标

### 核心任务

1. 家庭饮食在默认份量不变时，通过选择常用食材完成记录，不要求重复输入文字。
2. Keep 跟练或跑步结束后，只填写当前场景需要的数据，并可复用最近一次训练。
3. 打开今日页即可看到一个紧凑的当前阶段入口，阶段状态从正式记录自动汇总，不增加每日打钩。

### 成功标准

- 常用家庭饮食可以做到“打开、选择、保存”，只有份量变化或临时食物才需要输入。
- 蛋白质结果始终标记为估算，并能区分已估算、未估算和数据不足。
- Keep、跑步／Apple Watch、其他运动三条路径不展示彼此无关的字段。
- 当前阶段最多包含两个行动重点，不挤压今日页四类记录入口的首屏位置。
- 新能力不增加一级导航，不恢复完整营养数据库、复杂计划系统或打卡评价。

## 四、本轮范围

### 1．个人常用食材库

- 用户在应用内新增、编辑、排序和移除个人常用食材，不直接编辑 JSON 文件。
- 每项至少保存名称、分类、默认份量和单位。
- 蛋白质计算为可选能力；启用后保存参考份量、蛋白质克数、数据来源和适用的生重／熟重／可食部分口径。
- 包装食品优先使用用户从实际营养标签录入的数值；普通食材可以使用带来源的公共参考值；不知道数值时仍可作为“仅记录”食材。
- 食材库随完整备份迁移，不写入仓库文件，也不依赖运行时网络服务。

### 2．饮食快速记录与蛋白质助手

- 打开表单后继续自动选择餐次和当前所选日期。
- 表单先展示常用食材多选，再展示已选食材、当天份量调整、自由文字补充和本餐蛋白质估算。
- 常用食材选择器按肉蛋豆类、主食、奶类与饮品、蔬菜、水果和其他分区；标签显示名称、默认份量以及按该份量估算的蛋白质，缺少参考值时明确显示未设置。奶类与饮品只在界面合并，底层分类仍分别保存。
- 手机录入时在标题右侧只读显示当前记录日期，不再重复展示日期选择控件；餐次、食材选择、已选份量和自由文字区域使用紧凑间距，默认自由文字为三行，同时保持 44px 触控目标、16px 输入字号和窄屏无横向滚动。
- 所选日期已有饮食时，首页入口显示“记录／补充饮食”，表单用紧凑餐次标签载入原记录。早餐、午餐和晚餐出现同日重复时，保存前必须明确选择补充原记录或仍新增一条；加餐继续允许多条。该规则不修改 schema，也不拒绝已有重复记录。
- 选择食材时使用默认份量；用户可以修改本次份量，修改只影响当前记录。
- 保存时继续生成清晰的 `content` 原文，同时保存食材和蛋白质快照；修改食材库默认不能改变历史记录。用户明确把蛋白质参考值变更作为纠错、预览影响并确认同步时，可以修正同一来源且单位／口径兼容的历史蛋白质估算。
- 外卖、堂食和临时食物可以完全使用自由文字；未结构化部分不自动解析，不伪装成已测量营养值。
- 饮食录入时在本餐蛋白质估算旁显示早餐、午餐和晚餐的建议范围；有活动健康阶段目标时按阶段每日目标分配，没有时使用当前三餐工作范围作为临时预览。加餐不套用三餐目标，也不要求额外补齐数字；该提示不写入饮食记录。
- 趋势页按已有估算样本展示每日总量、分餐分布和未估算样本数；缺少结构化数据的 v10 饮食只显示“未估算”。

### 3．运动场景分流

- 运动表单先选择“Keep 跟练、跑步／Apple Watch、其他运动”。
- Keep 跟练记录课程名称、实际时长、现有主观强度、是否完整跟完，以及可选器械重量、不适和备注。
- 支持“重复上次训练”，复制后必须允许编辑，不能直接生成正式记录。
- 跑步／Apple Watch 展示时长、来源、距离、平均心率和配速预览。
- 其他运动保留类型、时长、强度和备注，不展示设备或课程字段。
- Keep 课程来源与 Apple Watch 数据来源保持正交，不复用同一个字段表达。
- 内置引导训练降为“备用文字训练”次级入口，现有草稿、撤销、替代动作和总结能力在本轮不重构。

### 4．当前健康阶段

- 产品内使用“当前阶段”，不使用容易与项目开发文件混淆的 Roadmap 名称。
- 同一时间最多一个活动阶段，建议周期为 2～4 周；阶段最多包含两个行动重点和若干只读观察项。
- 首批只支持蛋白质工作范围、每周力量次数和每周有氧次数，不建设通用目标规则引擎。
- 今日页只提供紧凑入口；饮食和运动卡在各自上下文中展示相关阶段状态。
- 阶段进度只根据正式饮食和运动记录计算，不增加手动完成按钮、连续打卡或惩罚性评分。
- 阶段结束时汇总蛋白质估算覆盖、分餐情况、实际训练次数、7 日均重、睡眠样本和主观不适；只展示数据和样本量，不自动诊断或生成下一阶段。

## 五、页面连接

### 今日

- 保留当前日历和四类记录入口的优先顺序。
- 增加一个不挤压首屏记录按钮的紧凑阶段入口。
- 饮食卡可以展示当日蛋白质估算和未估算状态。
- 运动卡可以展示本周实际力量和有氧次数。
- 今日训练卡保留每周模板，将主按钮改为普通运动记录入口或场景选择；备用文字训练作为次级动作。

### 饮食表单

1. 自动餐次和日期。
2. 常用食材多选。
3. 已选食材及本次份量调整。
4. 本餐蛋白质估算和三餐建议范围提示。
5. 自由文字补充。
6. 保存正式记录。

食材库为空时，继续允许直接填写原文，并提供“添加第一个常用食材”；食材保存失败时不得把新食材显示为已保存。

### 运动表单

1. 选择运动场景。
2. 展示场景专属高频字段。
3. 可选复用最近一次同场景训练。
4. 确认实际完成内容。
5. 保存正式记录。

### 设置

- 增加“我的常用食材”和“当前阶段设置”。
- 设置首页优先展示完整备份导出、最近备份状态和恢复入口，其后展示分析导出；完整备份导出和分析导出直接执行。
- 常用食材、每周训练模板、恢复备份和安装更新进入独立二级页面，二级页面返回设置首页；恢复继续保留文件选择、摘要预览和最终替换确认。

### 趋势

- 饮食卡增加蛋白质估算样本和分餐分布。
- 运动卡继续展示实际次数和时长，并补充当前阶段范围内的力量／有氧次数。
- 阶段结束摘要作为二级详情，不新增一级导航。

## 六、关键状态与边界

- 空食材库：自由文字记录始终可用。
- 食材无蛋白质值：可以选择和保存，标记为未估算。
- 单位不匹配：不得把按个参考值用于克数，也不得静默换算生重和熟重。
- 外卖堂食：允许完全不估算；当天汇总显示未估算样本，不能输出确定性缺口。
- 修改或移除食材：默认不改变已经保存的饮食快照；修改蛋白质参考值时提供仅影响以后／同步修正历史的显式选择，禁止静默回算，单位或口径变化不允许批量修正。
- 重复上次训练：复制到草稿，用户确认后才保存。
- 阶段无数据：显示样本不足，不把缺少记录判断为未完成。
- 本地写入失败：界面回滚，保留用户尚未保存的输入并显示错误。
- 无账号或权限状态：产品没有账号系统，不虚构无权限页面。
- 加载状态：本地同步读取通常不展示加载动画；迁移、导入等真实耗时操作才显示处理中状态。

## 七、数据方向

产品版本仍称 v11，但切片三因 schema v11 已经推送而使用 schema v12。正式字段和校验规则以 `DATA_MODEL.md` 为准：

- 根对象增加个人常用食材和健康阶段集合。
- 饮食保留 `content`，并增加可选食材快照；快照包含实际份量、口径、蛋白质估算和来源，不只保存食材库引用。
- 蛋白质使用整数最小单位持久化，界面负责克数显示，避免浮点累计误差。
- 运动使用 `keep`、`running`、`other`、`guided` 场景；Keep 详情、Apple Watch `source` 和引导快照彼此独立。
- 当前阶段固定支持蛋白质范围、力量次数和有氧次数，不接受任意表达式。
- 所有新增持久化对象继续使用 UUID、严格字段校验和 ISO 8601 时间戳。

### v11／v10 迁移要求

- 支持有效 v11 本地数据和完整备份迁移到 v12；缺少 v11 时继续支持有效 v10 链式迁移，完整保留四类正式记录和每周训练模板。
- v11 运动按已有事实映射场景：引导快照对应 `guided`，普通跑步对应 `running`，其余对应 `other`；不得猜测 Keep。
- v10 饮食只迁移原文，不自动生成食材或蛋白质快照。
- 先完整校验来源版本，再生成并校验 v12；只有 v12 写入成功后才切换应用状态。
- 保留原 v11 和已有 v10 存储键；迁移写入失败时展示迁移后的只读视图，允许下载原始来源数据并向用户说明。
- v12 完整备份导入前继续展示摘要并执行整体校验，覆盖前保护当前本地数据。

## 八、纵向切片实施顺序

### 切片一：数据基础

状态：已实现并验证。

- 冻结 schema v11 字段和校验规则。
- 实现 v10 本地数据与备份迁移、失败回滚和备份往返测试。
- 先用纯函数覆盖食材份量、蛋白质估算和阶段统计，不修改页面交互。

### 切片二：家庭饮食闭环

状态：已实现并验证；蛋白质趋势仍按原计划留到切片五。

- 实现食材库管理、饮食多选、份量调整、快照保存、今日汇总和记录编辑。
- 用一个完整家庭饮食流程验收后再扩展趋势，不同时铺开所有页面。

### 切片三：运动场景闭环

状态：实现与自动化验证完成，最终 390×844 浏览器回归待浏览器连接恢复；由于 schema v11 已经推送，本切片使用 schema v12 并迁移 v11／v10，避免在同一版本内静默改变严格数据结构。

- 实现 Keep、跑步／Apple Watch、其他运动分流和重复上次训练。
- 将引导训练入口降级，但不改动其内部状态机。

### 切片四：当前阶段闭环

状态：数据结构和统计纯函数已建立，界面待开发。

- 实现一个活动阶段、今日紧凑提醒、自动汇总和阶段结束摘要。
- 只消费已有正式记录，不创建第二套完成状态。

### 切片五：趋势、导出与整体验收

状态：待开发。

- 补齐蛋白质趋势、阶段摘要、完整备份和分析导出。
- 验证 390×844 手机视口、离线更新、触控尺寸、表单字号和存储失败回滚。

每个切片独立完成语法检查、相关自动化测试、手机视口检查和 `git diff --check`，只有验证通过后才更新 `ROADMAP.md` 的已完成状态。

## 九、明确不做

- 热量、脂肪和碳水完整追踪。
- 公共大型食物库、菜谱、拍照识别或运行时营养 API。
- 从自由文字自动推导确定性营养值。
- 多级目标、任务清单、连续打卡、徽章、计划执行率和系统推送。
- 体重期限预测、自动调整目标或医学结论。
- Apple Health 自动同步。
- 重写内置引导训练。

## 十、开发授权记录

用户已确认进入 v11 正式开发，并先后授权家庭饮食和后续开发；本轮按既定边界实施运动场景闭环。当前阶段和趋势导出仍保持独立切片，不因本轮实现扩大边界。
