# HealthLife Roadmap

## 当前阶段

当前正式代码已升级为 schema v12，切片一“数据基础”和切片二“家庭饮食闭环”已完成；常用食材新增／编辑已迁移到独立紧凑弹窗，支持输入法可视高度跟随与焦点自动定位。修改蛋白质参考值时现已默认只影响以后记录，也可在影响预览后主动同步修正单位／口径兼容的历史估算并短时撤销。设置页已按使用频率重组为紧凑首页和二级页面，完整备份、恢复入口和分析导出优先；新增饮食表单已处理长食材名称在手机窄屏下的换行与收缩，避免内容撑开页面，并在本餐蛋白质估算旁显示早餐、午餐和晚餐建议范围及缺口提示。常用食材选择器现按类型分区，标签显示默认份量和本份蛋白质估算，奶类与饮品仅在界面合并展示；手机新增饮食表单进一步将日期与餐次并排并压缩录入间距，保持窄屏无横向滚动。有效 v11 本地数据和完整备份会优先迁移，缺少 v11 时才链式迁移 v10，原前序版本本地键保留；迁移写入失败时进入只读保护。运动现已按 Keep、跑步和其他运动分流，支持复用最近一次同场景训练，备用文字训练降为次级入口。GitHub Pages 仍待单独确认。

## 已完成

- 确定产品定位、MVP 边界和三个一级入口：“今日、趋势、记录”。
- 确定运动、饮食、睡眠和体重四类核心记录范围。
- 确定饮食 MVP 使用“餐次＋一段文字描述”，暂不在录入时计算营养。
- 确定本地优先、完整 JSON 备份恢复、GitHub Pages 和 PWA 的技术方向。
- 建立项目级 `AGENTS.md`，明确数据、隐私、统计、验证和发布约束。
- 建立本路线图作为项目进度事实源。
- 初始化本地 Git 仓库，默认分支为 `main`。
- 配置 GitHub SSH 远端 `git@github.com:Gexiansen/HealthLifeRecording.git`；初始化时远端为空。
- 建立 `.gitignore`，排除环境变量、依赖、生成产物、编辑器配置、AI 本地配置和真实健康备份。
- 创建项目规范、路线图和忽略规则的初始本地提交。
- 将 `main` 首次推送到 GitHub 远端并建立上游跟踪关系。
- 建立 `docs/`、`tests/` 和 `test-data/` 目录约定，并补充当前开发与验证方式。
- 建立 `README.md` 和 `DATA_MODEL.md`，明确本地数据边界、schema v1 字段、范围、单位和唯一性规则。
- 实现 schema v1 根对象及设置、运动、饮食、睡眠、体重和每日饮水数据校验。
- 实现跨日睡眠时长、7 日均重、严格 JSON 序列化和解析纯函数。
- 使用 Node.js 标准库建立 11 项自动化测试，覆盖有效数据、未知版本、未知字段、无效日期、字段越界、跨日睡眠、日期唯一、全局 UUID 唯一、7 日均重、时间戳和序列化往返。
- 建立 `docs/index.html`、`styles.css`、`app.js`、`data.js` 和 `storage.js`，保持原生 HTML、CSS、JavaScript 与本地相对资源。
- 完成“今日、趋势、记录”三段导航；趋势页在本阶段只展示后续功能边界。
- 完成运动、饮食、睡眠、体重和饮水的新增、编辑及日期归属；睡眠、体重和饮水按日期自动进入已有记录编辑。
- 完成按日期倒序的全部记录列表、编辑入口、删除及 10 秒撤销。
- 使用版本化单一 `localStorage` 键保存 schema v1；读取异常时保留原始内容、锁定写入并提供下载入口。
- 完成保存失败界面回滚；数据只有在持久化成功后才替换当前应用状态。
- 完成周一开始的七天周视图和固定六周月视图，未来日期不可记录，历史日期可直接进入当日摘要和补录。
- 日历按运动、饮食、睡眠、体重和饮水五类展示每日完成数，并与新增、编辑和删除实时同步。
- 增加连续记录天数；口径为每天至少存在一类记录，今天尚未记录时保留截至昨天的连续结果。
- 增加最近 7 天和最近 30 天趋势窗口，按用户本地自然日确定统计边界。
- 体重趋势展示原始样本、最新值、区间变化和基于已有称重日的 7 日均重，不用缺失日期补零。
- 睡眠汇总展示样本数、平均时长和主观质量；运动汇总展示次数、总时长和类型分钟分布。
- 饮食汇总按有饮食记录的天数计算覆盖率，并展示健康程度和饱腹感均值；饮水日均只使用有记录日期。
- 所有趋势在无数据时显示明确空样本，并保留“不构成健康诊断”的边界说明。
- 增加 schema v1 完整 JSON 导出，以及格式版本、导出时间和全部设置／记录数据的备份封装。
- 增加备份文件严格解析、整体校验、日期范围与分类数量摘要预览，确认后完整替换本地数据。
- 覆盖非空或损坏本地数据前先触发当前内容下载；主数据写入失败时不切换应用状态。
- 增加最近备份时间、从未备份、超过 14 天及新增至少 10 条记录的提醒。
- 建立纯虚构的 `test-data/healthlife-demo-v1.json`，覆盖完整导入和数据摘要验证。
- 增加 PWA manifest、192／512 像素本地图标和版本化 Service Worker 应用外壳。
- Service Worker 安装时绕过普通 HTTP 缓存抓取当前资源，新缓存激活后清理旧版本；导航在线优先、断网回退缓存页面。
- 完成首次在线加载、停止本地服务器后离线重载、本地记录恢复和 390px 手机视口回归。
- 将阶段三至阶段六的 4 个提交推送到 `origin/main`，本地与远端均指向 `62a5148`。
- 历史补录标题、饮水标签和睡眠归档说明统一使用当前所选日期语境，日历所选日期增加可访问状态。
- 今日五类记录卡压缩为紧凑布局；增加运动 15／30／45／60 分钟快捷值和饮水 250／500 ml 直接累加。
- 主观评分增加文字语义，睡眠表单实时预览时长；表单关闭前保护未保存内容，保存按钮适配移动键盘和安全区。
- 删除入口收纳到每条记录的“更多”菜单并保留撤销；记录页增加类型、月份组合筛选、吸顶日期标题和可行动空状态。
- 趋势周期改为 7 天／30 天分段按钮，空数据合并引导；体重数据点支持查看详情，各模块在样本充分时展示与等长上一周期的差值。
- 备份恢复预览明确展示当前与导入记录数量；数据管理增加安装引导，Service Worker 更新时提示刷新并保留本地数据。
- PWA 应用外壳缓存提升至 v4，新增交互纯函数进入离线资源清单。
- 将移动端交互优化提交 `67c6921` 推送到 `origin/main`。
- 移除首页日历下方重复的精确日期输入，保留周／月日历选择和“回到今天”；PWA 应用外壳缓存提升至 v5。
- 移除首页日历下方重复的日期与完成数信息行；为旧缓存脚本保留不占布局的隐藏升级占位，PWA 应用外壳缓存提升至 v6。
- 建立 schema v2 数据模型与独立 `healthlife:data:v2` 存储；不读取、不迁移且不删除旧 schema v1 数据。
- 实现内置食物目录、自定义食品、最近使用和收藏排序，以及按克数保存餐食营养快照。
- 实现自制菜谱按原料重量和成品熟重折算每 100 克营养。
- 实现精确称重／外食估算模式和高／中／低可信度，精确模式拒绝低可信度。
- 实现热量、蛋白质、脂肪和碳水化合物的餐次、每日、7 天／30 天汇总与上周期蛋白质对比。
- 实现面向后续健康分析的按日 JSON 导出，覆盖餐食明细、营养、运动、睡眠、体重和饮水。
- 完整备份格式升级至 v2，并建立纯虚构的 `test-data/healthlife-demo-v2.json`。
- PWA 应用外壳缓存提升至 v7，新增营养计算和分析导出模块进入离线资源清单。
- 建立 schema v3 数据模型与独立 `healthlife:data:v3` 存储；不读取、不迁移且不删除旧 schema v1／v2 数据。
- 水煮鸡蛋支持按个录入，可调整每个可食部分克数，并在餐食快照中保存个数、单位和单个克数。
- 运动记录增加跑步类型、手动／Apple Watch 数据来源、活动热量、平均／最高心率和距离；配速由时长与距离计算。
- 增加每日步数记录、今日摘要、记录筛选、7 天／30 天趋势和上周期日均步数对比。
- 分析 JSON 增加运动设备摘要、配速、每日步数和餐食原始份量；完整备份和虚构演示数据升级至 v3。
- PWA 应用外壳缓存提升至 v8。
- 建立 schema v4 与独立 `healthlife:data:v4` 存储；有效 schema v3 数据自动迁移，原 v3 键保留，v1／v2 继续不读取。
- 增加周一至周日训练模板，默认周二力量 A、周四力量 B、周六跑走结合。
- 增加正常工作日、三档工作日加班、周末加班和休息日类型，并按当天工作约束生成训练、晚餐与恢复建议。
- 首页增加“今日计划”，支持当天训练覆盖、计划中／完成／缩短／改期／休息状态和改期目标日期。
- 每周模板和每日计划进入 v4 完整备份；v3 完整备份导入时自动转换为 v4，分析 JSON 升级并包含计划状态。
- PWA 应用外壳缓存提升至 v10，并加入健康计划模块。
- 建立 `UX_INTERACTION.md`，系统记录当前信息架构、页面、表单、计划、趋势、数据安全和 PWA 交互，并完成 390×844 手机端 UX 审查。
- 建立 schema v5 与独立 `healthlife:data:v5` 存储；按当前无正式数据的前提，不读取或迁移旧 schema。
- 未来日期开放计划编辑，同时禁用六类健康记录和快捷饮水；跨日期保存后自动切换到实际保存日期。
- 改期计划建立来源／目标双向关联，目标日期已有训练时要求确认覆盖，并通过整体校验拒绝孤立或链式关系。
- 计划与实际运动记录保持分离，同时在首页和计划表单提示匹配记录与状态不一致。
- 日历增加计划标记，记录页增加健康计划历史和筛选，趋势页增加计划执行汇总。
- 当天计划、每周模板、自定义食品和菜谱统一增加未保存内容保护。
- 顶部“数据”入口调整为“设置”，恢复预览增加计划数量、每周模板和工作类型，备份提醒覆盖计划变化。
- 快捷饮水增加 10 秒撤销；撤销按钮和体重图命中区域达到 44px，首页计划卡完成密度压缩。
- 完整备份升级为 v5，分析导出升级为 v3，PWA 应用外壳缓存提升至 v13。
- 建立六项新手动作库，覆盖壶铃杯式深蹲、哑铃罗马尼亚硬拉、俯卧撑、单臂哑铃划船、死虫式和爬楼梯，并为每个动作提供准备姿势、三条关键提示、常见错误和停止条件。
- 增加动作适应、全身力量 A、全身力量 B 和初级爬楼梯四套引导模板；首页计划卡按当天计划突出推荐模板。
- 实现全屏引导式训练，支持逐组完成、实际次数／楼层和负重、组间倒计时、跳过动作、提前总结以及退出后继续。
- 训练草稿使用独立版本化本地键，每组保存成功后界面才前进；结束总结确认后才生成正式运动记录，计划状态不自动改变。
- 建立 schema v6 和独立存储键，有效 v5 数据自动迁移且保留旧键；正式运动记录可保存引导训练动作、组次、负重和主观用力感快照。
- 完整备份升级为 v6并兼容迁移 v5 备份，分析导出升级为 v4，PWA 应用外壳缓存提升至 v14。
- 引导训练按实际执行动作展示最近三次历史；历史包含日期、组次、负重和已记录不适，不把替代动作混入原计划动作的历史。
- 最近两次同动作都完整完成、用力适中、明确反馈无不适时才建议使用器械最小重量档或每组增加 1 次；未反馈、样本不足、缩短、吃力或存在不适时维持、减量或停止。
- 首组开始前可选择同动作模式替代动作，草稿和正式记录同时保存计划动作与实际动作；当前替代覆盖徒手深蹲、臀桥、上斜俯卧撑、鸟狗式和轻松快走。
- 训练总结按动作区分“未记录”“没有不适”和具体不适，具体反馈保存固定部位与 1～3 级程度；趋势仅汇总已记录不适，不根据部位诊断损伤。
- 建立 schema v7 和独立存储键，有效 v6 数据自动迁移、v5 数据链式迁移且旧键保留；训练草稿升级至 v2 并迁移有效 v1 草稿。
- 完整备份升级为 v7 并兼容 v5／v6，分析导出升级为 v5，PWA 应用外壳缓存提升至 v15。
- 完成产品强力收缩：移除工作排班、加班约束、当天计划状态、改期、计划历史／趋势、步数、饮水、连续记录、饮食健康评分、活动热量和最高心率。
- 建立 schema v8 和独立 `healthlife:data:v8` 存储键，不读取或迁移 v1 至 v7 数据和备份；每周训练模板简化为根字段，只用于当天推荐。
- 首页、趋势、记录、设置、完整备份和分析 JSON 均回归运动、饮食、睡眠、体重四类核心数据；引导式训练、动作替换、历史样本、保守进阶建议和不适反馈继续保留。
- 删除独立计划模块 `docs/planning.js` 及其测试，PWA 应用外壳缓存提升至 v16。
- 引导式训练增加独立版本化撤销历史；完成一组、跳过动作和提前结束前保存草稿快照，刷新后仍可连续撤销。
- 训练、休息和总结阶段统一提供上一步恢复；总结阶段显示“返回修改”，可回到提前结束前的实际组次继续训练。
- 放弃训练和提前结束增加二次确认；确认放弃只清除当前草稿与撤销历史，不影响正式健康记录，PWA 应用外壳缓存提升至 v17。
- 将每周模板收敛为力量 A、力量 B、跑走结合和休息；新增与跑走模板对应的 4 轮跑走交替引导，爬楼梯保留为训练选择器中的独立模板。
- 移除设置数据模型中的 `weightUnit` 和 `goalWeightGrams`，建立 schema v9 与 `healthlife:data:v9` 独立存储键；当前不读取、不迁移 v1 至 v8 数据。
- 移除无正式数据阶段不需要的 v8 本地／备份兼容层，完整备份只接受 v9；同步更新设置、数据模型、备份、UX 文档和相关测试。
- 统一今日训练入口文案为“选择训练”，更新分析导出和页面壳版本至 v21，并补充周模板、v9 单格式校验和页面壳回归测试。
- 将饮食记录收敛为餐次加一段 `content` 原文，移除精确／估算模式、可信度、饱腹感、食物选择、食物库、菜谱和录入时营养计算。
- 建立 schema v10 与 `healthlife:data:v10` 独立存储键，不读取、不迁移 v1 至 v9；完整备份升级为 v10，分析导出升级为 v8，PWA 应用外壳缓存提升至 v22。
- 趋势和首页饮食摘要改为餐数、记录天数和日期覆盖率，保留原文供后续人工分析；同步更新数据模型、UX、README、项目规范和测试。
- 首页调整为日期、四类记录、今日训练的顺序，确保 390×844 首屏完整展示四个记录入口；手动运动隐藏 Apple Watch 详情，记录表单按类型聚焦高频输入，PWA 应用外壳缓存提升至 v23。
- 日历标题增加所选日期训练模板类型和所选月份实际运动天数；运动天数按正式运动记录的自然日去重，不引入执行率或计划状态，PWA 应用外壳缓存提升至 v24。
- 建立本地私有健康建议历史文档约定：讨论形成明确结论后自动按日期追加个人背景、饮食、训练与恢复建议，临时方案不写入；真实内容由 Git 忽略，不进入公开仓库。
- 建立 `PRODUCT_DEFINITION_V11.md`，确认个人常用食材、饮食蛋白质估算、运动场景分流和当前健康阶段四个下一版本方向；采用“文字基线＋纵向切片”的验证方式，不为现有原生页面重复制作独立原型。
- 建立 schema v11、`healthlife:data:v11` 和 v11 完整备份格式；严格校验个人常用食材、饮食快照和健康阶段，支持有效 v10 本地数据／完整备份迁移并保留原 v10 键，迁移写入失败时阻止编辑并提供原始数据下载。
- 重建与 DOM、存储无关的蛋白质估算纯函数：按食材参考份量计算整数毫克，明确区分已估算、部分估算和未估算；自由文字不自动解析，食材单位与生熟口径不静默换算。
- 完成个人常用食材管理和家庭饮食快捷记录：支持新增、编辑、排序、应用内确认移除、默认份量多选、本餐份量调整、自由文字兜底、实时蛋白质预览、历史快照编辑和今日汇总；修改或移除食材默认不回算历史餐食。
- 建立单一活动健康阶段的数据契约和正式记录统计纯函数，为后续阶段界面保留蛋白质覆盖、力量次数和有氧次数基础，不增加每日打钩状态。
- 同步升级数据模型、产品定义、UX、README 和项目规范，PWA 应用外壳缓存提升至 v25。
- 建立 schema v12、`healthlife:data:v12` 和 v12 完整备份格式；优先迁移经过完整校验的 v11，缺少 v11 时链式迁移 v10，迁移后保留原键，当前或优先前序键损坏时不错误回退。
- 普通运动表单按 Keep、跑步和其他运动分流；Keep 保存课程、完成情况、可选器械重量和整体不适，跑步保存可选距离与 Apple Watch 平均心率，其他运动不展示课程或设备字段。
- 新建运动支持把最近一次同场景训练复制到未保存表单，不复制平均心率、不适和备注；首页今日训练以普通记录为主操作，原引导式训练改为“备用文字训练”。
- 分析导出保留运动场景与 Keep 详情，PWA 应用外壳缓存提升至 v26；数据、交互、备份、页面外壳和引导场景回归均已纳入自动化测试。
- 将常用食材新增／编辑从设置列表底部迁移到独立二级弹窗；窄屏使用全高单列表单、顶部标题和底部吸附操作区，关闭与取消继续保护未保存内容。页面壳回归覆盖弹窗边界，PWA 应用外壳缓存提升至 v27。
- 将食材弹窗压缩为名称单行、基础信息三列、蛋白质自然语言同排输入和可折叠来源说明；标题、字段滚动区和操作栏分层，使用可视视口高度和最小滚动距离保持焦点可见。新增滚动距离纯函数与页面壳回归，PWA 应用外壳缓存提升至 v28。

## 进行中

- 设置数据操作分层：已实现设置紧凑首页、完整备份与分析数据直接导出，以及恢复、常用食材、每周训练模板和安装更新二级页面；返回／系统返回优先回首页，关闭设置和离开未保存模板继续受保护，PWA 应用外壳已提升至 v30。自动化验证已完成，待补 390×844 首屏可见性、二级返回和未保存确认的真实浏览器验收。
- v11 切片三“运动场景闭环”：补做 390×844 手机视口回归和备用文字训练正式保存的最终浏览器验收；实现与自动化验证已经完成。
- 常用食材紧凑弹窗：补做 390×844 真实手机视口下的新增、编辑、未保存确认，以及展开蛋白质后逐项输入的软键盘可达性验收；实现与自动化验证已经完成。
- 蛋白质参考值历史纠错：已在 schema v12 现有快照字段内实现影响预览、默认仅影响以后、兼容历史同步修正、单位／口径拦截、写入失败保护和 10 秒整体撤销，数据与页面契约自动化验证通过，PWA 应用外壳已提升至 v29；补做 390×844 真实手机视口下的二层确认弹窗、仅影响以后、同步修正、禁止口径变更回算和撤销流程验收后，才能移入“已完成”。

## 待办

### v11 纵向切片

1. 当前阶段闭环：完成单一活动阶段、最多两个行动重点、今日紧凑提醒、正式记录自动汇总和阶段结束摘要。
2. 趋势、导出与整体验收：补齐蛋白质趋势、阶段摘要和结构化分析导出，并完成离线更新与失败回滚整体验收。

每个切片均须在用户明确授权后实施，并独立通过语法检查、相关自动化测试、390×844 手机视口回归和 `git diff --check`，不得提前把未验证能力写入“已完成”。

### 公开发布

- 将 GitHub Pages 发布源设置为 `main` 分支的 `/docs` 目录，并核对正式站点首次加载与离线更新。

### 后续评估

- CSV 导出。
- Apple Health、智能手表或其他健康平台接入。
- 多设备同步或多人共享。
- v11 上线后连续观察 2～4 周的饮食记录耗时、蛋白质估算覆盖、训练记录耗时和阶段提醒负担，再决定是否继续扩展。

以上后续评估能力不属于 v11，只有在真实使用反馈明确后再进入需求设计。

## 阻塞

- 浏览器控制当前返回“无可用浏览器”；设置数据操作分层、切片三、常用食材独立弹窗和蛋白质历史纠错的 390×844 最终交互仍需在浏览器连接恢复后补验。代码、迁移和自动化测试不受影响。

## 最近验证

- 2026-08-10：设置数据操作分层通过 `npm run check`；自动化测试共运行 81 项，81 项通过、0 项失败，覆盖首页操作优先级、二级页面边界、返回与未保存保护、恢复安全流程和 PWA v30 外壳；`git diff --check` 通过。浏览器发现结果仍为空，未把 390×844 首屏与真实返回交互标记为完成。
- 2026-08-10：蛋白质参考值历史纠错通过 `npm run check`；自动化测试共运行 79 项，79 项通过、0 项失败，覆盖完整历史修正、单位／口径拦截、部分兼容快照、页面选择和 PWA v29 外壳；静态核对 189 个页面 ID 无重复，应用引用的 172 个 ID 无缺失，`git diff --check` 通过。浏览器发现结果仍为空，未把 390×844 二层弹窗和撤销交互标记为完成。
- 2026-08-10：常用食材紧凑弹窗通过 `npm run check`；自动化测试共运行 75 项，75 项通过、0 项失败；`git diff --check` 通过。浏览器发现结果仍为空，真实软键盘与 390×844 交互未标记为完成。
- 2026-08-10：常用食材独立弹窗通过 `npm run check`；自动化测试共运行 73 项，73 项通过、0 项失败；`git diff --check` 通过。浏览器发现结果为空，未把 390×844 交互验收写成已完成。
- 2026-08-10：schema v12 和运动场景切片通过 `npm run check`；自动化测试共运行 72 项，72 项通过、0 项失败；`git diff --check` 通过。
- 2026-08-10：桌面应用闪退前，真实浏览器已验证 v11→v12 本地迁移，以及 Keep、跑步、其他运动新增、同场景复用、Keep 编辑和记录详情展示；平均心率、不适和备注未随复用带入。该轮浏览器宽度不是最终 390px 验收宽度。
- 2026-08-10：桌面应用恢复后本地代码和测试改动完整保留；浏览器连接不可用，因此未把切片三整体标记为最终验收完成。

- 2026-07-30：引导式训练第二阶段 JavaScript 语法检查通过；自动化测试共运行 82 项，82 项通过、0 项失败，`git diff --check` 通过。
- 2026-07-30：在 390×844 手机视口验证力量 B 的哑铃罗马尼亚硬拉替换为臀桥，退出并刷新后替代选择仍从 v2 草稿恢复；完成一组并提前总结后，膝盖 2／3 不适成功保存。
- 2026-07-30：再次选择臀桥时显示同动作历史，并因上次明显不适提示不加量；7 天运动趋势汇总 1 次已记录不适和膝盖 1 次。页面宽度和滚动宽度均为 390px，最小可见按钮高度 44px，控制台无警告或错误。

- 2026-07-22：核对 `AGENTS.md` 与 `ROADMAP.md`，两份文件对产品范围、本地数据边界、饮食记录精度、页面导航、技术栈和分阶段交付的描述一致。
- 2026-07-22：通过 SSH 只读检查确认 GitHub 远端可访问且没有既有引用；本地仓库已初始化为 `main`，`origin` 地址与用户提供的地址一致。
- 2026-07-22：初始本地提交包含 `AGENTS.md`、`ROADMAP.md` 和 `.gitignore`，未包含真实健康数据或秘密文件。
- 2026-07-22：获得用户明确确认后，将本地 `main` 推送到 `origin/main` 并建立上游跟踪关系。
- 2026-07-22：`node --check docs/model.js` 通过；`node --test tests/*.test.js` 共运行 11 项测试，11 项通过、0 项失败。
- 2026-07-22：阶段二 JavaScript 语法检查通过；新增数据变更、存储和页面外壳测试后共运行 22 项，22 项通过、0 项失败。
- 2026-07-22：在 390×844 手机视口完成运动、饮食、跨日睡眠、体重和饮水新增，体重编辑，删除撤销、日期切换、三段导航及刷新持久化回归。
- 2026-07-22：手机视口宽度和页面滚动宽度均为 390px，表单输入字号为 16px，最小可见按钮高度为 44px；浏览器控制台无警告或错误。
- 2026-07-22：损坏 JSON、存储读取不可用和写入失败通过标准库测试；真实浏览器损坏数据下载流程留待阶段五与完整恢复一起验收。
- 2026-07-23：阶段三语法检查通过；新增 5 项日历和连续记录测试后共运行 27 项，27 项通过、0 项失败。
- 2026-07-23：在 390×844 手机视口验证周视图 7 天、月视图 42 格、历史日期直达、五类状态和当天补录；连续记录从截至昨天的 1 天正确更新为包含今天的 2 天。
- 2026-07-23：月视图下页面宽度和滚动宽度均为 390px，控制台无警告或错误。
- 2026-07-23：阶段四语法检查通过；新增 3 项趋势统计测试后共运行 30 项，30 项通过、0 项失败。
- 2026-07-23：在 390×844 手机视口验证 7 天／30 天切换、体重样本图、睡眠、运动、饮食和饮水汇总；当前样本正确显示 68.6 kg、7 小时 40 分、45 分钟、7 天覆盖 14％和饮水日均 1650 ml。
- 2026-07-23：30 天趋势页面宽度和滚动宽度均为 390px，周期选择框字号为 16px，控制台无警告或错误。
- 2026-07-23：阶段五语法检查通过；新增完整备份、提醒元数据和虚构演示备份测试后共运行 35 项，35 项通过、0 项失败。
- 2026-07-23：在 390×844 手机视口使用真实文件选择器导入虚构备份，摘要正确显示 8 条记录及分类数量，确认后记录页完整恢复 8 条；控制台无应用警告或错误。
- 2026-07-23：导出操作与备份提醒状态更新已在浏览器验证；内置浏览器未捕获 Blob 下载事件，实际下载文件仍需在最终浏览器回归中复核。
- 2026-07-23：阶段六语法检查通过；新增 manifest、应用外壳清单和 Service Worker 注册测试后共运行 38 项，38 项通过、0 项失败。
- 2026-07-23：本地服务器日志确认 Service Worker 使用网络加载了页面、全部 JavaScript 模块、样式、manifest 和两种尺寸图标。
- 2026-07-23：停止本地服务器后在 390×844 视口重载成功，页面宽度与滚动宽度均为 390px，记录页仍显示 8 条虚构本地记录，应用控制台无错误。
- 2026-07-24：确认本地 `main` 与 `origin/main` 均指向 `62a5148`；公开启用 GitHub Pages 尚未执行。
- 2026-07-24：在 390×844 视口完成今日、运动／饮食／睡眠表单、趋势、记录和数据管理交互走查，确认日期语境、首屏密度、评分语义、误删除风险、空状态和移动安全区为本轮优化重点。
- 2026-07-24：交互纯函数、趋势上周期比较和 PWA 清单测试加入后，JavaScript 语法检查通过，自动化测试共 44 项，44 项通过、0 项失败。
- 2026-07-24：在 390×844 视口验证历史日期标题、饮水快捷增加、运动快捷时长、睡眠时长预览、未保存保护、备份替换数量、记录筛选、删除收纳、趋势空状态和体重数据点详情。
- 2026-07-24：从旧缓存真实升级至 v4 后，本地 9 条虚构测试记录保持可用；页面宽度与滚动宽度均为 390px，所有可见按钮不低于 44px，控制台无警告或错误。
- 2026-07-24：获得用户明确确认后，将移动端交互优化提交 `67c6921` 推送到 `origin/main`。
- 2026-07-24：移除首页“精确选择”后，JavaScript 语法检查通过，自动化测试共 45 项，45 项通过、0 项失败；在 390×844 视口验证历史日期选择、42 格整月视图和“回到今天”，页面无横向溢出，所有可见按钮不低于 44px，控制台无警告或错误。
- 2026-07-24：移除首页日期与完成数信息行后，JavaScript 语法检查通过，自动化测试共 45 项，45 项通过、0 项失败；验证并修复 v5 旧脚本与 v6 新页面短暂混用时的空元素错误。390×844 视口下隐藏占位高度为 0，历史日期切换和“回到今天”正常，页面无横向溢出，所有可见按钮不低于 44px，控制台无警告或错误。
- 2026-07-29：饮食营养追踪实施后，`node --check docs/*.js`、`git diff --check` 通过；自动化测试共 52 项，52 项通过、0 项失败。
- 2026-07-29：静态核对应用脚本引用的 101 个页面元素 ID，全部可在 `docs/index.html` 中找到。
- 2026-07-29：核对 USDA FoodData Central 的官方数据说明，确认其食物营养值以每 100 克可食部分为基础；内置值明确标记为会受品牌、部位和烹饪含水量影响的参考估算。
- 2026-07-29：schema v3 实施后，`node --check docs/*.js`、`git diff --check` 通过；自动化测试共 56 项，56 项通过、0 项失败。
- 2026-07-29：静态核对应用脚本引用的 112 个页面元素 ID 和页面中的 129 个唯一 ID，未发现缺失或重复。
- 2026-07-30：轻量健康计划实施后，`npm run check`、`git diff --check` 通过；自动化测试共 63 项，63 项通过、0 项失败，覆盖 schema v3 到 v4 迁移、每日计划严格校验、模板覆盖、加班建议、备份迁移和分析导出。
- 2026-07-30：静态核对应用脚本引用的 126 个页面元素 ID 和页面中的 145 个唯一 ID，未发现缺失或重复。
- 2026-07-30：在 390×844 视口验证 3.5 小时加班建议、每周模板保存、历史日期模板应用、手动改期、刷新持久化和 PWA v10 更新提示；页面无横向溢出，可见按钮不低于 44px，表单字号不低于 16px，控制台无警告或错误。
- 2026-07-30：基于 `acd48a1` 完成 UX 交互文档和手机端审查；确认未来日期计划受阻、跨日期保存反馈冲突、改期目标无接续、计划与实际记录可能不一致、部分弹窗无未保存保护等优先问题。
- 2026-07-30：390×844 实测日期面板约 307px、计划卡约 202px，页面无横向溢出；删除后的撤销按钮高度为 40px，低于 44px 项目标准；浏览器控制台无警告或错误。
- 2026-07-30：schema v5 与两阶段 UX 优化完成后，`npm run check`、`node --check docs/analysis.js`、`node --check docs/nutrition.js` 和 `git diff --check` 通过；自动化测试共 63 项，63 项通过、0 项失败。
- 2026-07-30：静态核对应用脚本引用的 136 个页面元素 ID 和页面中的 156 个唯一 ID，未发现缺失或重复。
- 2026-07-30：在 390×844 视口验证未来日期只开放计划、计划与模板未保存保护、改期冲突确认、来源／目标双向显示、计划历史、跨日期保存跳转和快捷饮水撤销；页面宽度与滚动宽度均为 390px，可见按钮不低于 44px，计划卡由约 202px 压缩至约 156px，控制台无警告或错误。
- 2026-07-30：引导式训练第一阶段完成后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 72 项，72 项通过、0 项失败，覆盖动作库、逐组推进、跳过与缩短、训练快照、草稿恢复、schema v5 到 v6 迁移、v5 备份迁移和分析导出。
- 2026-07-30：静态核对应用脚本引用的 169 个页面元素 ID 和页面中的 189 个唯一 ID，未发现缺失或重复。
- 2026-07-30：在 390×844 视口验证壶铃深蹲模板选择、8 kg 三组完成、休息跳过、退出和 PWA 更新刷新后的断点恢复、训练总结确认、正式运动记录及记录页动作摘要；页面无横向溢出，训练按钮不低于 44px，输入字号为 16px，控制台无警告或错误。
- 2026-07-31：产品强力收缩完成后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 45 项，45 项通过、0 项失败，覆盖 schema v8 严格校验、拒绝 v1 至 v7、四类记录、备份、分析导出、趋势、PWA、页面壳和 v16 资源版本隔离。
- 2026-07-31：在 390×844 视口验证四类首页卡、`0/4` 日历状态、简单今日训练、运动保存、可选饮食饱腹感和四项趋势；页面宽度与滚动宽度均为 390px，可见控件不低于 44px，表单字号不低于 16px，新版本页面控制台无警告或错误。
- 2026-07-31：引导式训练误触恢复完成后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 48 项，48 项通过、0 项失败，覆盖连续撤销、刷新持久化、草稿匹配、撤销键清理、确认弹窗和 v17 资源版本隔离。
- 2026-07-31：在 390×844 视口验证完成首组后刷新仍可撤销、提前结束取消与确认、总结返回第 2 组、放弃训练取消与确认；放弃后原有 1 条正式运动记录保持不变，新增训练操作栏显示正常，控制台无警告或错误。
- 2026-08-01：每周模板与 schema v9 实施后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 50 项，50 项通过、0 项失败，覆盖跑走引导、模板选项收敛、跑走记录类型、v1 至 v8 拒绝和 v21 资源版本隔离。
- 2026-08-01：在 390×844 视口验证“选择训练”、跑走结合 4 轮引导、退出／放弃草稿和设置模板选项；页面宽度与滚动宽度均为 390px，可见按钮不低于 44px，页面控制台无警告或错误。
- 2026-08-03：饮食文字记录 v10 实施后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 50 项，50 项通过、0 项失败，覆盖 v10 数据模型、v1 至 v9 拒绝、文字饮食导出、趋势覆盖率、页面表单和 v22 资源版本隔离。当前运行环境未提供可用浏览器实例，390×844 交互走查待下次具备浏览器时补做。
- 2026-08-07：睡眠新建表单默认入睡时间调整为 22:30、起床时间调整为 06:30，并补充页面默认值回归测试。
- 2026-08-07：核心录入体验优化后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 54 项，54 项通过、0 项失败，覆盖首页记录入口优先级、Apple Watch 按需展示、表单初始焦点和 v23 资源版本隔离。
- 2026-08-07：390×844 视口下，首页首屏完整可见的记录入口由 1／4 提升到 4／4，睡眠和体重入口所需滚动分别由 78px、156px 降为 0；手动力量运动弹窗由 802px 降至 624px，手动跑步弹窗由 854px 内容高度降至 624px 且无内部溢出。页面无横向溢出，可见按钮不低于 44px，表单字号不低于 16px。
- 2026-08-08：日历轻量训练上下文完成后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 56 项，56 项通过、0 项失败，覆盖月度运动天数去重、日历标题页面壳和 v24 资源版本隔离。
- 2026-08-08：在 390×844 视口验证今日“跑走结合”、历史周五“休息”和跨月份统计切换；同日保存 2 条运动后仍统计为 1 个运动日。日期面板保持 307px，四个记录按钮均完整位于底部导航上方，页面宽度与滚动宽度均为 390px，控制台无警告或错误。
- 2026-08-09：确认 `HEALTH_ADVICE_HISTORY.local.md` 命中 `.gitignore`，真实个人健康背景和建议没有出现在 Git 工作区变更中；同步规则调整为稳定事实或明确结论自动追加，临时方案不写入，历史修正不覆盖原记录。
- 2026-08-09：完成 `PRODUCT_DEFINITION_V11.md` 与项目规范交叉核对；产品定义未包含真实个人健康信息，未创建独立原型目录，正式 `docs/` 页面未修改，`git diff --check` 通过。
- 2026-08-09：v11 切片一和切片二完成后，`npm run check`、`npm test` 和 `git diff --check` 通过；自动化测试共 65 项，65 项通过、0 项失败，覆盖 v10 本地数据／备份迁移、失败只读保护、严格 v11 校验、食材管理、蛋白质估算、饮食快照、阶段统计、页面壳和 v25 离线资源。
- 2026-08-09：静态核对应用脚本引用的 147 个页面元素和页面中的 163 个唯一 ID，未发现缺失或重复；隐私扫描未发现个人体重、体脂或年龄数据进入受版本控制文件。
- 2026-08-09：在 390×844 视口完成虚构常用食材新增、180 g 本餐份量调整、自由文字补充、54 g 部分蛋白质估算、保存、刷新恢复和编辑回填；把食材库参考值从每 100 g 30 g 改为 25 g 后，历史餐食仍保持 54 g 快照。页面宽度与滚动宽度均为 390px，饮食弹窗无横向溢出，可见按钮不低于 44px，输入字号不低于 16px。
- 2026-08-10：复现并修复长食材名称导致的新增饮食表单横向溢出；食材选项和已选食材行增加窄屏收缩、任意位置换行和网格子项最小宽度约束。`npm run check`、`npm test`（81 项通过）和 `git diff --check` 通过；在 320×640 与 390×844 视口选中长中文及连续英文食材名称后，文档与页面滚动宽度均分别保持 320px、390px，页面无横向溢出。PWA 应用外壳版本提升至 v31。
- 2026-08-10：饮食表单新增按餐次的蛋白质建议提示；早餐 30～40 g、午餐和晚餐 35～45 g，活动阶段存在蛋白质目标时按每日范围分配，加餐不套用三餐建议。提示仅用于录入时辅助，不改变正式记录、历史快照或统计口径。`npm run check`、`npm test`（82 项通过）和 `git diff --check` 通过；在 390×844 视口验证三餐切换、低于／处于／高于范围、加餐边界和页面无横向溢出，控制台无警告或错误。PWA 应用外壳版本提升至 v32。
- 2026-08-10：新增饮食表单常用食材按“肉蛋豆类、主食、奶类与饮品、蔬菜、水果、其他”分区；标签第二行显示按默认份量估算的本份蛋白质和生重／熟重口径，无参考值时显示未设置。界面仅合并 `dairy`／`drink`，底层分类、饮食快照和蛋白质计算不变。`npm run check`、`npm test` 和 `git diff --check` 通过；在 390×844 视口验证分区标签、两行高度和页面无横向溢出，控制台无警告或错误。PWA 应用外壳版本提升至 v33。
- 2026-08-11：新增饮食表单改为手机紧凑布局，日期与餐次并排，说明输入默认为三行，食材选择和已选份量区域压缩间距；关联到 `meal-form` 的餐次字段仍参与表单重置、脏状态检测和保存。`npm run check`、`npm test` 和 `git diff --check` 通过；390×844 与 320×640 视口回归验证页面滚动宽度分别保持 390px、320px，横向溢出为 0。PWA 应用外壳版本提升至 v34。
