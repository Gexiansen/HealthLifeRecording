# HealthLife UX 交互文档

## 产品目标

HealthLife 帮助个人以尽可能低的负担记录运动、饮食、睡眠和体重，并在数据积累后导出给分析工具使用。它不是日程、考勤、任务管理或医疗诊断产品。

## 信息架构

一级导航固定为：

1. 今日：日期选择、四类摘要、快速记录、今日训练推荐；页面顺序优先保证记录入口可见。
2. 趋势：7 天／30 天的体重、睡眠、运动和饮食汇总。
3. 记录：按日期展示四类明细，支持筛选、编辑和删除。

设置是辅助入口，包含个人常用食材、每周训练模板、备份、恢复、分析导出和 PWA 安装。

## 今日

- 默认展示本周日历，可展开整月；日历是首页唯一日期选择入口。
- 日历标题紧凑展示所选日期的每周训练模板类型，以及所选月份已有正式运动记录的运动天数；运动天数按自然日去重，同日多次运动只计 1 天，不展示计划执行率。
- 每天的完成数按四类记录统计，显示为 `0/4` 至 `4/4`。
- 未来日期可以查看训练模板，但不能新增健康记录或开始新训练。
- 首页固定按“日期、四类记录、今日训练”排列，让运动、饮食、睡眠和体重入口在 390×844 首屏完整可见。
- 四张摘要卡分别对应运动、饮食、睡眠和体重。睡眠、体重已有当天记录时直接进入编辑；运动、饮食允许新增多条。
- “今日训练”只读取周一至周日模板。主操作“记录实际训练”进入普通运动场景表单；“备用文字训练”作为次级入口保留现有逐组引导。休息日仍允许记录实际运动，但不记录计划完成状态。
- 每周模板只提供力量 A、力量 B、跑走结合和休息；爬楼梯等独立训练仍通过模板选择器进入，不占用每周模板选项。

## 引导式训练

1. 用户从今日训练的“备用文字训练”进入模板选择，推荐模板只作为提示。
2. 每个动作显示准备姿势、三条关键提示、常见错误和停止条件。
3. 用户逐组填写实际次数／楼层／分钟和可选负重，完成后进入休息。
4. 首组前可替换为同动作模式的低门槛动作；历史分别保存原计划动作和实际动作。
5. 中途退出后从本地草稿继续。跳过或退出不会生成正式记录。
6. 结束页展示实际完成摘要、主观用力感和可选不适反馈；确认后才生成正式运动记录。
7. 完成组、跳过动作或提前结束后，可以撤销上一步；撤销历史随训练草稿保存在本地，刷新页面后仍然有效。
8. 总结页使用“返回修改”恢复到上一步，允许连续返回到需要修正的位置。
9. “提前结束并总结”需要确认；“放弃本次训练”需要二次确认，并且只清除本次草稿和撤销历史。

历史样本只提供保守的维持、减量或小幅进阶提示，不自动修改本次目标，也不诊断损伤。

## 记录表单

### 运动

普通运动先选择场景，表单只展示该场景需要的字段：

- Keep 跟练：课程名称、训练类型、完成情况、可选器械重量、时长、强度、数据来源、可选平均心率、整体不适反馈和备注。Keep 课程与 Apple Watch 来源使用不同字段表达。
- 跑步：固定跑步类型，填写时长、强度、数据来源、可选距离、平均心率和备注；配速根据当前时长与距离即时预览。
- 其他运动：只填写类型、时长、强度和备注，不展示课程或设备字段。

新建时按所选日期的每周模板默认选择场景：力量 A／B 对应 Keep，跑走结合对应跑步，休息对应其他运动。用户可以切换。每个普通场景可以把最近一次同场景记录复制到当前未保存表单；课程、类型、时长、强度、来源、器械重量和跑步距离可复用，平均心率、不适反馈和备注不复制。编辑现有记录时不显示复用入口。

备用文字训练生成独立 `guided` 场景。普通场景选择器不展示它；编辑该记录时保留动作快照，只允许修改日期、整体时长、强度和备注。

### 饮食

用户选择餐次后，优先从个人常用食材中多选，也可以只写自由文字：

- 常用食材使用设置中的默认份量；选中后可以调整本餐份量，修改不会回写食材库。
- 已配置蛋白质参考值的食材实时显示本餐估算；无参考值食材和非空自由文字明确计为未估算内容。
- 保存时同时生成可读 `content` 和食材历史快照；移除食材和普通信息修改不会改变历史餐食，修改蛋白质参考值默认也只影响以后记录。
- 食材库为空、吃外卖、堂食或旅行时，自由文字始终可用，不要求先创建食材，也不自动解析营养。
- 编辑旧餐食时恢复当时的食材名称、份量和参考值，不用当前食材库静默回算。只有用户在食材参考值变更时查看影响预览并明确选择同步，才修正兼容的历史蛋白质估算。

### 睡眠

填写入睡、起床、质量、可选夜醒次数和备注，界面实时预览跨日时长。入睡和起床时间相同会阻止保存。

### 体重

填写公斤体重，可选体脂率和备注。同一日期再次进入时编辑原记录。

## 趋势与记录

- 趋势仅显示四张核心卡。体重显示原始样本和 7 日均重；睡眠显示平均时长与质量；运动显示时长、类型、可选设备摘要和不适汇总；本切片的饮食趋势仍显示餐数、记录天数和日期覆盖率，蛋白质趋势留到 v11 切片五。
- 没有样本时显示“暂无足够数据”，不把缺失日期当作零。
- 记录页只显示正式健康记录，不混入训练模板或计划状态。
- 删除入口收纳在“更多”，成功后提供短时撤销；本地写入失败时界面不假装成功。

## 设置与数据安全

- 个人常用食材支持新增、编辑、排序和移除；新增／编辑使用独立二级弹窗，不在食材列表底部展开表单。移动端弹窗占满当前可视区域，标题和保存操作保持可见，打开后直接聚焦名称。
- 食材基础信息使用紧凑布局：名称独占一行，分类、默认份量和单位同排。启用蛋白质估算后，以“每多少单位含多少克蛋白质”同排输入，口径与来源同排，来源说明默认折叠且编辑已有说明时自动展开。
- 关闭、取消、点击遮罩或使用系统返回键时，如果食材内容已修改，必须先确认是否放弃；删除前使用应用内确认弹窗，历史饮食快照不受影响。
- 已有食材的蛋白质参考定义发生变化且命中历史记录时，保存前展示二次选择：默认“仅影响以后记录”；“同步修正历史估算”同时展示可修正餐数、日期范围和旧总量到新总量。单位或生熟口径变化时禁用同步并说明原因；部分历史快照不兼容时明确显示未修改餐数。确认同步后只更新蛋白质估算和引用元数据，成功提示提供 10 秒撤销，保存失败则停留在确认界面且不得显示成功。
- 每周模板固定七天，只影响今日推荐。
- 完整备份使用 schema v12；有效 v11 或 v10 本地数据和备份可以完整校验后迁移，v1 至 v9 不迁移。
- 首次本地迁移成功后保留原 v11 和已有 v10 键；迁移写入失败时进入只读状态并允许下载原始前序版本数据。
- 导入先展示日期范围、四类数量、食材数、阶段数和每周模板，再整体替换。
- 覆盖非空数据前先下载当前完整备份。
- 分析 JSON 输出四类健康数据，其中饮食保留原文，不把文字转换成未经确认的营养数值；不包含已移除的计划、步数或饮水。

## 移动端验收

- 主要视口为 390×844。
- 页面不得横向溢出。
- 高频触控目标不小于 44px。
- 表单输入字号不低于 16px，避免移动浏览器自动缩放。
- 运动、饮食和体重表单打开后分别聚焦已选运动场景、首个常用食材（没有时聚焦自由文字）和体重；睡眠表单聚焦标题，不主动唤起时间选择器。
- 弹窗应可滚动，并保护未保存内容。
- 常用食材弹窗使用固定标题、独立滚动字段区和固定底部操作区；输入法改变可视高度后同步调整弹窗，当前输入项被遮挡时只自动滚动所需距离，不要求用户手动寻找输入位置。

## 本轮删减结论

已移除工作排班、加班约束、每日计划状态、训练改期、步数、饮水、连续记录、计划历史／趋势、饮食健康评分、活动热量和最高心率。这些功能增加了首页和数据模型的认知负担，却没有直接强化“记录四类健康数据并复盘”的核心闭环。
